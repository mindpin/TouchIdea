<!DOCTYPE html>
<html xmlns:wb="http://open.weibo.com/wb">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Touch Idea - 内测版仅供体验，用户数据可能会不定期清空" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Touch Idea - 内测版仅供体验，用户数据可能会不定期清空" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= yield(:head) %>
    <%= csrf_meta_tags %>
    <script src="http://tjs.sjs.sinajs.cn/open/api/js/wb.js?appkey=<%= ENV['SINA_APP_KEY'] %>&debug=true" type="text/javascript" charset="utf-8"></script>
  </head>
  <body data-spy="scroll" data-target=".bs-docs-sidebar">
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <div class="container" id="container">
        <div id="messages">
          <%= render 'layouts/messages' %>
        </div>
        <%= yield %>
      </div>
      <% if current_user %>
        <script type="text/javascript">
          var user_id = '<%= current_user.id %>';
        </script>
      <% end %>
      <% if Rails.env.production? %>
        <div class="hidden">
        <script src="https://s4.cnzz.com/z_stat.php?id=1254445939&web_id=1254445939" language="JavaScript"></script>
        </div>
      <% end %>
    </main>
  </body>
</html>
